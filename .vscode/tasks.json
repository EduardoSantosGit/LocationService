{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/src/LocationService.Api/LocationService.Api.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}